{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Ejecutar archivo abierto",
            "skipFiles": ["<node_internals>/**"],
            "program": "${file}"
        }
    ]
}
